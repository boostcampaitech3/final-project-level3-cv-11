params = {
    "PROCESS_TARGET": "vid", # "img", "vid", "cam" 
    # "IMAGE_DIR": parse.image_dir, 
    "BBOX_THRESHOLD": 30, # Threshold of bounding box
    "RECOG_THRESHOLD": 0.8, # Threshold of recognition
    # "INPUT_MODE": parse.input_mode,
    # "DETECTOR": parse.detector,
    "NUM_WORKERS": 4, # Number of workers
    "RANDOM_SEED": 42, # Random seed for data split
    # "DEVICE": parse.device,
    # "SAVE_DIR": parse.save_dir, 
    # "PRETRAINED_PATH": parse.pretrained_path,
    "DEBUG_MODE": False # print on off
}